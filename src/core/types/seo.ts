export interface SeoData {
  title: string;
  description: string;
  ogImage: string;
}

interface SeoDefaults {
  siteName: string;
  defaultTitle: string;
  defaultDescription: string;
  organizationName: string;
}
